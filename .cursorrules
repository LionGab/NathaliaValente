# ClubNath VIP - React Native + Expo + Supabase + TypeScript

## 🎯 CONTEXTO DO PROJETO
- **App**: ClubNath VIP - Comunidade exclusiva da Nathália Valente
- **Influenciadora**: 35M seguidores, CEO NAVA, R$3-4M receita anual
- **Estratégia**: Monetização passiva via licenciamento de imagem
- **Target**: Mães, zero fricção de download (PWA), experiência premium

## Stack Tecnológica
- React Native 0.74+ / Expo SDK 51+
- TypeScript (strict mode)
- Supabase (auth, database, realtime, storage)
- React Query (TanStack Query) para data fetching
- NativeWind para styling
- React Navigation 6.x
- React Hook Form + Zod para validação
- PWA (Progressive Web App) para zero fricção

## Estrutura de Pastas
src/
├── api/           # React Query hooks
├── components/ui/ # Design system components
├── features/      # Feature-based modules
├── lib/supabase/  # Supabase client e helpers
├── navigation/    # Navigation config
├── types/         # TypeScript types
└── utils/         # Utility functions

## Regras CRÍTICAS (nunca viole)

### ANTES de criar QUALQUER arquivo novo
1. SEMPRE busque arquivos similares em @components/, @hooks/, @services/
2. Se encontrar código similar, REUTILIZE ou ESTENDA ao invés de duplicar
3. NUNCA crie arquivos com nomes: Component2, utils-new, service-copy
4. PERGUNTE se devo reutilizar código existente ou criar novo

### Convenções de Código
- Componentes funcionais com hooks (zero classes)
- TypeScript strict, sem 'any'
- Imports absolutos: @/components não ../../../
- camelCase para funções/variáveis
- PascalCase para componentes
- Indentação: 2 espaços
- Sempre use StyleSheet.create() para estilos

### React Native Específico
- SafeAreaView para compatibilidade iOS
- Platform.OS para código específico de plataforma
- KeyboardAvoidingView para formulários
- FlatList com windowSize otimizado para listas longas
- Testes em iOS E Android obrigatórios

### Supabase Patterns
- Sempre use React Query para queries do Supabase
- Políticas RLS documentadas em cada query
- Erro handling consistente com try/catch
- Auth state gerenciado por Context
- AsyncStorage para session persistence

### Prevenção de Duplicatas
- Antes de criar hook, verificar @hooks/
- Antes de criar service, verificar @lib/supabase/
- Antes de criar componente UI, verificar @components/ui/
- Se similar existe, PARE e pergunte antes de criar novo

### Git Workflow
- Branches: feature/TASK-123-descricao
- Commits: [TASK-123] Descrição clara
- SEMPRE rodar testes antes de commit
- NUNCA commit com console.log em produção

### Qualidade Obrigatória
- Zero warnings do ESLint
- Zero erros do TypeScript
- Cobertura mínima de testes: 80%
- Componentes novos = testes novos
- Error boundaries em todas as screens

## O que NÃO fazer

❌ Adicionar elementos de UI não solicitados
❌ Modificar áreas de layout não mencionadas
❌ Criar soluções complexas quando simples funciona
❌ Duplicar código existente
❌ Usar 'any' no TypeScript
❌ Pular tratamento de erros
❌ Esquecer acessibilidade (labels, roles)
❌ Commitar código com placeholders/TODOs

## Comandos de Desenvolvimento
npm start              # Inicia Expo dev server
npm run ios            # iOS simulator
npm run android        # Android emulator
npm test               # Roda testes
npm run lint           # ESLint
npm run type-check     # TypeScript check

## Antes de Responder
1. Analise código existente em @Codebase
2. Verifique se funcionalidade já existe
3. Identifique padrões estabelecidos no projeto
4. Proponha solução que segue os padrões
5. Mencione se está reutilizando ou criando código novo